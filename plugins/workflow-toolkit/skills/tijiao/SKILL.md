---
name: tijiao
description: >
  Alias for /commit. Commits and pushes staged changes with Chinese
  Conventional Commits and optional SemVer tagging. "提交"的完整拼音别名。
disable-model-invocation: true
---

本 skill 是 `/commit` 的拼音别名（提交 → tijiao）。

请调用 `/commit` skill 执行完整的提交工作流，将以下参数透传：

$ARGUMENTS
