---
name: xiufu
description: >
  Alias for /fix-ci. Tracks CI pipeline failures and iteratively fixes issues
  until all green. "修复"的完整拼音别名。
disable-model-invocation: true
---

本 skill 是 `/fix-ci` 的拼音别名（修复 → xiufu）。

请调用 `/fix-ci` skill 执行完整的 CI 修复工作流，将以下参数透传：

$ARGUMENTS
