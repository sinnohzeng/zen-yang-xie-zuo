---
name: shouwei
description: >
  Alias for /wrap-up. Session-end orchestrator: syncs docs, captures lessons,
  commits, tracks CI. "收尾"的完整拼音别名。
disable-model-invocation: true
---

本 skill 是 `/wrap-up` 的拼音别名（收尾 → shouwei）。

请调用 `/wrap-up` skill 执行完整的会话收尾工作流，将以下参数透传：

$ARGUMENTS
