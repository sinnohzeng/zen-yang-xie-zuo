---
name: yanzheng
description: >
  Alias for /verify-done. Verifies work completion by running tests and applying
  senior engineer review lens. "验证"的完整拼音别名。
disable-model-invocation: true
---

本 skill 是 `/verify-done` 的拼音别名（验证 → yanzheng）。

请调用 `/verify-done` skill 执行完整的验证工作流，将以下参数透传：

$ARGUMENTS
