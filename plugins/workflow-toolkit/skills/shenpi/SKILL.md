---
name: shenpi
description: >
  Alias for /review-plan. Reviews implementation plans from UX, frontend engineering,
  and architecture perspectives. "审批"的完整拼音别名。
disable-model-invocation: true
---

本 skill 是 `/review-plan` 的拼音别名（审批 → shenpi）。

请调用 `/review-plan` skill 执行完整的计划评审工作流，将以下参数透传：

$ARGUMENTS
